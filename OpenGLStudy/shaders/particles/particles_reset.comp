#version 440

layout (local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

#include "particles_common.glsl"

void main()
{
	imageStore(particleMapImage, ivec3(gl_GlobalInvocationID.xyz), uvec4(NULL_ID));
}